namespace
↓
define onde a classe pertence

use
↓
permite referenciar a classe por um nome curto

autoload
↓
encontra/carrega o arquivo da classe


PSR-4 faz o mapeamento entre namespace e estrutura de diretórios.